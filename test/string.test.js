
describe("String API: ", () => {
  describe("#isPalindrome()", () => {
    it("keepsmiling.isPalindrome() should return a boolean", () => {
      assert(typeof keepsmiling.isPalindrome("ewrwerwerwer") === "boolean");
    });
  });
});
